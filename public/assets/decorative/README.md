# Decorative Assets

Place UI decorative motifs and textures here (grain, halftone, motifs, overlays).

Rules:
- Decorative assets should not contain canonical content data.
- Keep files optimized for layered atmosphere rendering.
